"""`notify` — rendu des gabarits, envoi, limitation de débit.

Décide **comment** le message est rendu et **si le quota autorise l'envoi**,
jamais s'il faut alerter. N'importe que `core` (contrat D2).
"""
